<div class="map-page">

  <div class="container card-container">
    <h1 class="header-style">Tracks Near Me</h1>
    <div class="row">
      <div class="cards col-md-8 runners-list">
        
          <% @tracks.each do |track| %>
            <div class="card">
              <%= cl_image_tag track.image.key, crop: :fill, class: "card-img-top" %>
              <div class="card-body">
                <h4><%= track.keyword%></h4>
                <br>
                <%= track.description%>
             </div>
            </div>
          <% end %>
      </div>
      <div class="col-md-4 map">
        <div id="map"
              style="width: 100%; height: 80vh;"
              data-markers="<%= @markers.to_json %>"
              data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>


    </div>
  </div>
</div>
